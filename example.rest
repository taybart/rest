# Use variables!
set HOST localhost
set PORT 8080
set AUTH apikey1234

http://${HOST}:${PORT}
Authorization: Bearer ${AUTH}
GET /

---

# after 1 second call this
delay 500ms
http://${HOST}:${PORT}
Content-Type: application/json
POST /
{
  # Comment all the things!
  "ints": 123,
  "strings": ""
}

